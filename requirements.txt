# CARLA client (runs locally, connects to remote server)
carla==0.9.16

# Core dependencies
numpy>=1.24.0
opencv-python>=4.8.0
pygame>=2.5.0
Pillow>=10.0.0

# Deep learning (for PilotNet)
torch>=2.0.0
torchvision>=0.15.0

# For Alpamayo model (optional - requires 24GB+ VRAM)
transformers>=4.57.0
huggingface_hub>=0.20.0
accelerate>=0.25.0
# deepspeed>=0.17.0  # Optional: for faster inference

# Configuration
pyyaml>=6.0

# Visualization
matplotlib>=3.7.0

# Utilities
tqdm>=4.65.0

# WebSocket for browser connection (optional)
websockets>=12.0

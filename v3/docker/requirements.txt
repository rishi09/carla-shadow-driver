# Shadow Driver v3 - Python dependencies
# Install torch/torchvision separately via --index-url for CUDA builds

# CARLA Python API (must match simulator version)
carla==0.9.15

# WebSocket server
websockets==14.1

# Image processing
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# Config parsing
PyYAML==6.0.2

# ML model inference
# torch and torchvision installed separately with CUDA index
transformers==4.46.3
accelerate==1.1.1
